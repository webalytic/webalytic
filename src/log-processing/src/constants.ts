export enum HitType {
  PAGEVIEW = 'pageview',
  EVENT = 'event'
}

export enum HitDataSource {
  SDK = 'sdk',
  APP = 'app'
}

export enum HitSessionControl {
  START = 'start',
  END = 'end'
}

export enum HitProductAction {
  PURCHASE = 'purchase'
}

export enum TrafficSourceSystemValues {
  NONE = '(none)',
  DIRECT = '(direct)',
  NOT_SET = '(not set)',
  ORGANIC = 'organic',
  NOT_PROVIDED = 'not provided',
  REFERRAL = 'referral'
}

export enum ResourceType {
  WEB = 'web',
  IOS = 'ios',
  ANDROID = 'android'
}
